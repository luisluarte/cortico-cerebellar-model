library(dplyr)

# We need to simulate the traces for one subject and check the GLM differences
# But wait, we can just load the raw C++ functions and run it for a subject.
# To be faster, let me just check the magnitudes of mu in a quick script if I can.
